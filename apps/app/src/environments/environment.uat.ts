export const environment = {
  // ----------------------------------------------------------------
  production: true,
  name: 'uat',
  // ----------------------------------------------------------------
  logger_level: 5, // TRACE = 0, DEBUG = 1, INFO = 2, LOG = 3, WARN = 4, ERROR = 5, FATAL = 6, OFF = 7
  // ----------------------------------------------------------------
  api_base_path: '/api-uat'
  // ----------------------------------------------------------------
};
